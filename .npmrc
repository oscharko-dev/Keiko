# KEIKO-0314: fail `npm ci` closed on an install script no one has reviewed, instead of the
# default advisory warning that scrolls past in CI logs. The reviewed set lives in package.json
# `allowScripts`; scripts/check-install-script-approvals.mjs pins it to exact locked versions.
strict-allow-scripts=true
