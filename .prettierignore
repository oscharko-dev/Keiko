dist/
coverage/
node_modules/
package-lock.json
# Qodo Merge repository config; TOML has no Prettier parser.
.pr_agent.toml
.playwright-mcp/
test-results/
playwright-report/
docs/release/1209-bundle-evidence.json
docs/release/1209-perf-evidence.json
docs/release/1580-workspace-perf-evidence.json
.github/ISSUE_TEMPLATE/
.github/workflows/
docs/adr/
ui/
# Frozen, byte-sensitive proof/evidence artifacts (design-system fidelity + a11y proofs, release
# manifests). These are generated/curated release evidence — reformatting them would churn or break
# the SHA-referenced #1300/#1405 fidelity gates, so they are excluded like the release evidence above.
docs/design-system/evidence/
# Security regression matrix is a curated security-stream data artifact (content-validated by
# scripts/check-security-regression-matrix.mjs); keep its bytes stable, do not restyle it.
scripts/security-regression-matrix.json
# Canonicalized generated Codex app-server schema snapshots are byte/digest evidence. The
# schema verifier owns their formatting and rejects semantic or canonical-byte drift.
packages/keiko-server/resources/codex-app-server-schema/
# ESLint owns this file's bytes: it is regenerated by `--suppress-rule`/`--prune-suppressions`
# (audit KEIKO-0118 ratchet). Restyling it would churn on the next prune.
packages/keiko-ui/eslint-suppressions.json
