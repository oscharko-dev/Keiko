# Issue #1296 — editor-agent evidence

Generated by:

```sh
npm run test:e2e:editor-fidelity-1296
```

The Playwright gate starts the packaged CLI UI, opens a synthetic project in the running Monaco editor,
and drives the live inline-completion route. It captures the actual editor window in Dark, Light, and
High Contrast, plus `manifest.json` with resolved token and Monaco rendering evidence.

Artifacts:

- `dark-editor-agent.png`
- `light-editor-agent.png`
- `high-contrast-editor-agent.png`
- `manifest.json`

The running editor verifies that inline ghost text uses `--ed-agent-ghost`, which aliases `--ed-ghost`
without visual drift. Permissioned agent mutation flows remain separately governed by #1405.
