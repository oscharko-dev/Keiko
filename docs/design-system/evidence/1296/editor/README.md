# Issue #1296 — editor-agent evidence

Generated by:

```sh
npm run test:e2e:editor-fidelity-1296
```

The Playwright gate starts the packaged CLI UI path, then renders an editor-agent context scene with
the real product `packages/keiko-ui/src/app/globals.css`. It captures Dark, Light, and High Contrast
screenshots and writes `manifest.json` with the resolved token evidence.

Artifacts:

- `dark-editor-agent.png`
- `light-editor-agent.png`
- `high-contrast-editor-agent.png`
- `manifest.json`

The scene verifies that editor ghost text uses `--ed-agent-ghost`, which aliases `--ed-ghost` without
visual drift. Permission and sensitive-action prompts are included as token-backed primitives only;
live authority wiring remains deferred to #1405.
