# Docked-agent managed-LSP verification evidence record

This is the filled "Required evidence record" demanded by
[`docs/qa/docked-agent-managed-lsp-verification.md`](docked-agent-managed-lsp-verification.md#required-evidence-record)
for Epic #2094 child #2281. That document is the contract; this record is the proof that the
targeted suite was executed and what it does and does not cover.

## Run header

| Field    | Value                                                                                                                                                                                              |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Commit   | `c6bca92e5d24d099cd6480f440c10b97ab33648d` plus this issue's working-tree changes (uncommitted at record time). The final immutable candidate commit must rerun the command below before delivery. |
| Platform | macOS (Darwin) arm64, Node v22.22.3. Linux/CI is authoritative for release sign-off; this record is the local preflight, not a substitute for the required GitHub `ci` run.                        |
| Command  | `npx vitest run tests/editor-agent-managed-lsp.integration.test.ts --reporter=json`                                                                                                                |
| Result   | 1 file, 12/12 tests passed, 0 failed, 0 skipped                                                                                                                                                    |
| Artifact | `sha256:bb0cad7143e62e647780d832f9a6f86d9f3d1131b1d9e8534b4485f99f9d07a2` (JSON reporter output; counts and durations only, no request/response bodies)                                            |

Mutation controls: the same suite, with the fake-provider request assertions
(`fixture.spawnedMethods`) or the real-BFF/`EditorAgentHttpClient.action()` traversal removed, is
the negative control required by the verification contract's "Failure-first and mutation evidence"
section. It is exercised by code review rather than a committed always-failing test, matching this
repository's existing mutation-evidence convention for this suite.

## Language and operation matrix evidence

`Docked-agent test` cites the specific `it(...)` block in
`tests/editor-agent-managed-lsp.integration.test.ts` that dispatches the operation through the real
BFF and tool host. `Shared-infra proof` cites the epic-verified, language/operation-generic layer
(`hostLanguageOperation.test.ts`, `providerOperationMatrix.test.ts`) that proves per-operation LSP
dispatch correctness but does not exercise the agent-facing wrapper for that operation/language
pair.

| Operation       | Python                                                                                                         | Go                                                                                             |
| --------------- | -------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Diagnostics     | Docked-agent test: "resolves Python diagnostics and Go navigation..." — bounded, content-free audit            | Shared-infra proof only (limitation: no Go docked-agent diagnostics test)                      |
| Definition      | Shared-infra proof only (limitation: no Python docked-agent definition test)                                   | Docked-agent test: "resolves Python diagnostics and Go navigation..." — root-relative location |
| References      | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent references test)                       |
| Type definition | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent typeDefinition test)                   |
| Implementation  | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent implementation test)                   |
| Call hierarchy  | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent callHierarchy test)                    |
| Inlay hints     | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent inlayHints test)                       |
| Signature help  | Docked-agent test: "resolves references, hierarchy, inlay, and signature operations..."                        | Shared-infra proof only (limitation: no Go docked-agent signatureHelp test)                    |
| Prepare rename  | Docked-agent test: "resolves Python diagnostics and Go navigation..." — review-only, byte-identical file after | Shared-infra proof only (limitation: no Go docked-agent renamePrepare test)                    |
| Code actions    | Docked-agent test: "resolves Python diagnostics and Go navigation..." — review-only, byte-identical file after | Shared-infra proof only (limitation: no Go docked-agent codeActions test)                      |

Limitation: the composition proof (real BFF, real tool host, real fake-LSP transport) is
demonstrated for both languages, and the full read-only operation vocabulary is demonstrated for
Python. The remaining 8 Go operations are proven correct at the shared LSP-dispatch layer
(language/operation-generic) but not independently through the docked-agent path for Go. This
mirrors the Go/definition-only, Python/full-vocabulary split already present in the suite; closing
it fully would require adding Go coverage for 8 more operations, which is tracked as a follow-up
rather than expanded silently in this record.

## Effective-state and failure matrix evidence

| State or transition             | Docked-agent evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Active                          | Both language-lane tests above; `PROVIDER_UNAVAILABLE`-free succeeded outcomes, root-relative paths only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Disabled                        | "rejects a stale advertised capability after workspace deactivation without fallback" — `PROVIDER_UNAVAILABLE`, zero provider dispatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Policy blocked                  | "fails closed on a policy-blocked provider without dispatching to the fake process" — `PROVIDER_UNAVAILABLE`, zero provider dispatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Missing                         | "fails closed with PROVIDER_UNAVAILABLE when the activated tool is no longer resolvable, without spawning it" — activation stays `enabled`, the provisioned executable is removed before dispatch, `PROVIDER_UNAVAILABLE`, zero provider dispatch                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Unhealthy                       | "fails closed with TIMED_OUT when the provider is unhealthy from crash-loop exhaustion, never negotiating" — a spawn that crashes on every attempt drives the manager's own restart throttle to `RESTART_THROTTLED` (crash-loop exhaustion) end to end through the real BFF; `TIMED_OUT`, the operation's own method is never dispatched. The crash-loop resolves in milliseconds (no artificial backoff in the throttle), so this does not depend on the real 60s restart window; disposing the exhausted pool entry still costs the real `shutdownTimeoutMs` (~5s), which is why this test and the client fixture's HTTP timeout budget are longer than the others |
| Unsupported operation           | "fails closed with UNSUPPORTED_OPERATION when the negotiated capability set excludes the operation, without dispatching it" — negotiation completes (`initialize` dispatched) but the excluded operation's method is never sent, `UNSUPPORTED_OPERATION`                                                                                                                                                                                                                                                                                                                                                                                                             |
| Timeout                         | "fails closed with TIMED_OUT when the provider accepts the request but never answers it" — the operation's method is dispatched and never answered, the real per-request deadline elapses, `TIMED_OUT`, pending request discarded                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Cancel before dispatch          | Shared-infra proof only (`editor-agent-tool-host.test.ts`); no docked-agent-path test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Cancel in flight                | "propagates in-flight caller cancellation to the managed provider request" — `CANCELLED`, fake provider still received the request                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Truncation                      | "caps an over-limit references result at the docked-agent boundary without leaking the dropped tail" — a maxReferenceLocations-plus-one fake response is capped to the limit, `truncated: true`, and the dropped entry's marker is absent from both the tool output and the audit trail                                                                                                                                                                                                                                                                                                                                                                              |
| Workspace switch                | "rechecks managed activation after a workspace switch before provider dispatch" — new root re-checked, zero stale-root dispatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Stale activation revision       | "rechecks managed activation after a workspace switch..." and "rejects a stale advertised capability..." cover the revision/deactivation forms                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Stale capability snapshot       | Shared-infra proof only (`managedLspRoutes.test.ts`); no docked-agent-path test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Restart                         | Shared-infra proof only (`lspProcessManager.test.ts` stale-generation discard); no docked-agent-path test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Malformed or oversized response | Shared-infra proof only (`lspSecurity.test.ts`, `lspFrameCodec.test.ts`); no docked-agent-path test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

Redaction: every docked-agent test above asserts the serialized audit trail does not contain the
workspace root path or provider-returned diagnostic/label text (see the `serialized`/`toContain`
assertions in the cited `it` blocks). No test in this suite records source, stderr, executable
paths, or secrets in evidence. "keeps the resolved executable path and provider config directory
out of the queued payload, host output, and audit trail on both a succeeding and a fail-closed
call (AC3)" independently proves this for the resolved executable path and provider config
directory specifically, across both a succeeding and a fail-closed dispatch.

## Disposition

The docked-agent-path-specific rows above are release evidence. The "shared-infra proof only" rows
are epic-verified but not independently proven through this issue's product path; per
`docked-agent-managed-lsp-verification.md`, they are recorded here as explicit limitations rather
than converted into a claimed pass. No coverage floor, assertion, bound, or redaction rule was
lowered to produce this record.
