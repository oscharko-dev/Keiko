name = "keiko-for-quality"
main = "../../scripts/keiko-for-quality-worker.mjs"
compatibility_date = "2026-07-11"

[triggers]
# Two-minute liveness backstop (Issue #2507). Webhooks drive evaluation on evidence changes; each
# sweep re-evaluates only pull requests that can still move (pending, head-changed, or a settled
# verdict older than RECONCILE_BACKSTOP_MS), so settled pull requests are not re-checked every tick.
crons = ["*/2 * * * *"]

[[d1_databases]]
binding = "KEIKO_FOR_QUALITY_DB"
database_name = "keiko-for-quality"
database_id = "replace-with-production-d1-id"

[vars]
TARGET_REPOSITORIES_JSON = '[{"repository":"oscharko-dev/Keiko","baseBranch":"dev","profile":"keiko"},{"repository":"oscharko-dev/Keiko-Native","baseBranch":"main","profile":"keiko-native"}]'
STABILITY_WINDOW_MS = "60000"
RECONCILE_BACKSTOP_MS = "900000"
SOCKET_RISK_ALLOWLIST_JSON = '[]'
SOCKET_RISK_ACTORS_JSON = '[]'
