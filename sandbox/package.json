{
  "name": "@oscharko-dev/keiko-dev-sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Versioned local sandbox for running the current Keiko development build.",
  "scripts": {
    "refresh": "node ./scripts/refresh.mjs",
    "start": "node ./scripts/start.mjs",
    "stop": "node ./scripts/stop.mjs"
  },
  "dependencies": {
    "@oscharko-dev/keiko": "file:.."
  }
}
