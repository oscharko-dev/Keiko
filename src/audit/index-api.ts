// Re-export shim: the evidence index/list API lives in @oscharko-dev/keiko-evidence (issue #163,
// ADR-0019).

export { listEvidence, loadEvidence, type EvidenceListEntry } from "@oscharko-dev/keiko-evidence";
