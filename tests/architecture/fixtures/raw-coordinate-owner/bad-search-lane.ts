// H1 must compute raw coordinates inside the workspace owner. The public barrel
// used to let a coding-runtime caller select raw text without a guarded import.
export const invalidCodingSearchDependencies = { contentLane: "editor" };
